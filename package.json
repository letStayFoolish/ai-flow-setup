{
  "name": "ai-flow-setup",
  "version": "0.1.0",
  "description": "Pull your Claude Code AI-flow (CLAUDE.md, rules, skills, commands) into any project or machine via npx, with safe diff-based merging.",
  "type": "module",
  "license": "MIT",
  "author": "letStayFoolish",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/letStayFoolish/ai-flow-setup.git"
  },
  "bin": {
    "ai-flow-setup": "./dist/cli.js"
  },
  "files": [
    "dist",
    "catalog"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && mkdir -p dist/generated && cp src/generated/catalog.json dist/generated/catalog.json",
    "catalog:build": "tsx scripts/build-catalog.ts",
    "dev": "tsx src/cli.ts",
    "prepare": "npm run catalog:build && npm run build",
    "prepublishOnly": "npm run catalog:build && npm run build"
  },
  "dependencies": {
    "@clack/prompts": "^0.9.1",
    "commander": "^12.1.0",
    "diff": "^7.0.0"
  },
  "devDependencies": {
    "@types/diff": "^5.2.3",
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=18"
  }
}
